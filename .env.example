# Cloudflare deployment (CI/non-interactive)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# Spotify catalog resolution/enrichment (Phase 1.04+; keep server-side only)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_MARKET=US

# Only variables intentionally exposed to the browser should use NEXT_PUBLIC_.
